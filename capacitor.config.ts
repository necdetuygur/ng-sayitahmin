import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.necdetuygur.sayitahmin',
  appName: 'Sayı Tahmin Oyunu',
  webDir: 'dist/ng-sayitahmin',
  bundledWebRuntime: false
};

export default config;
